package abcd
/**
 * Like a hashtag. Suppose we create a hashtag called "Summer for Kids" and
 * associate it with half a dozen assets that are particularly relevant to
 * summertime activities for kids. And suppose we implement a RESTful API that
 * anyone can call, asking "give me the assets tagged with "Summer for Kids"
 * and our app returns the six assets.
 */
class Tag {

    String text

    static constraints = {
    }
}
